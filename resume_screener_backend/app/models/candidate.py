"""
STATUS: NOT BUILT YET - Pydantic response shape for a single candidate.
Currently candidates.py returns raw Mongo documents directly (dicts).
Define a proper schema here once the response fields are finalised, so
FastAPI can validate/document the response shape automatically.
"""
